# dependencies / build
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# env (Vercel 대시보드에 등록 — 커밋 금지)
.env
.env.local
.env*.local

# python 산출/로그/캐시 (로컬 전용)
# ★2026-08-09 패턴 확장 — 종전 `scripts/_dryrun/` 는 정확히 그 이름만 걸러서
#   `_dryrun_prod_vb0_2026-08-07` 같은 대조 산출 디렉터리가 계속 미추적으로 떴다.
scripts/_dryrun*/
scripts/*.log
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# 재검정 임시 스냅샷 (2026-08-12)
scripts/_dr_*/
