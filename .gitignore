# dependencies / build
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# env (Vercel 대시보드에 등록 — 커밋 금지)
.env
.env.local
.env*.local

# python 산출/로그/캐시 (로컬 전용)
# ★2026-08-09 패턴 확장 — 종전 `scripts/_dryrun/` 는 정확히 그 이름만 걸러서
#   `_dryrun_prod_vb0_2026-08-07` 같은 대조 산출 디렉터리가 계속 미추적으로 떴다.
scripts/_dryrun*/
# ⚠️★2026-08-19 ★같은 실패가 재발했다 — 위 주석이 「이름만 걸러서 놓쳤다」고 적어 둔
#   바로 그 문제인데, ★`_dry_k3_080/` 처럼 ★`_dryrun` 으로 시작하지 않는 이름이 12개 떴다.
#   ★★교훈: 접두어를 ★공통 최소 단위(`_dry`)까지 줄인다. ★한 글자라도 더 붙이면 다음에 또 샌다.
scripts/_dry*/
scripts/_dryrun*_HASHES.txt
scripts/*.log
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# 재검정 임시 스냅샷 (2026-08-12)
scripts/_dr_*/
